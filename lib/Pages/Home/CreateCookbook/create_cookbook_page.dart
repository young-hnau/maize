import 'package:flutter/material.dart';

class CreateCookbookPage extends StatelessWidget {
  const CreateCookbookPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Create Cookbook Page"),
      ),
    );
  }
}
