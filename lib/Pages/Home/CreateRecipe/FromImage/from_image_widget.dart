import 'package:flutter/material.dart';

class FromImageWidget extends StatelessWidget {
  const FromImageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("From Image"),
    );
  }
}
