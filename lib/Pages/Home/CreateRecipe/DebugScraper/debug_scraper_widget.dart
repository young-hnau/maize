import 'package:flutter/material.dart';

class DebugScraperWidget extends StatelessWidget {
  const DebugScraperWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Debug Scraper"),
    );
  }
}
