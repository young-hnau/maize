import 'package:flutter/material.dart';

class ImportZIPWidget extends StatelessWidget {
  const ImportZIPWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Import ZIP"),
    );
  }
}
